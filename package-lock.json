{
  "name": "getotp-render-server",
  "version": "4.0.0",
  "description": "GetOTP Server — Render deployment (replaces Firebase Hosting + Cloud Functions + Cloudflare Worker)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": "18"
  },
  "dependencies": {
    "express": "^4.21.0",
    "firebase-admin": "^12.7.0"
  }
}
