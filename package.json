{
  "name": "getotp-render-server",
  "version": "4.12.0",
  "description": "GetOTP Render Server - in-memory SMS/OTP store, SSE dashboard, FCM control",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node test/phone.test.js && node test/store.test.js && node test/latency.test.js && node test/forwarding.test.js"
  },
  "engines": {
    "node": "20"
  },
  "dependencies": {
    "express": "^4.21.0",
    "firebase-admin": "^12.7.0"
  }
}
