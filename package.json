{
  "name": "getotp-render-server",
  "version": "4.0.0",
  "description": "GetOTP Server — fully self-contained Render deployment, zero external dependencies",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": "20"
  },
  "dependencies": {
    "express": "^4.21.0"
  }
}
