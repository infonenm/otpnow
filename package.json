{
  "name": "getotp-render-server",
  "version": "4.5.0",
  "description": "GetOTP Render Server — self-contained + FCM for instant wake",
  "main": "server.js",
  "scripts": { "start": "node server.js" },
  "engines": { "node": "20" },
  "dependencies": {
    "express": "^4.21.0",
    "firebase-admin": "^12.7.0"
  }
}
